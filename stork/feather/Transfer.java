package stork.feather;

/**
 * A handle on the state of a data transfer. This interface contains methods
 * for controlling the transfer and monitoring transfer rate.
 *
 * @param <S> the source {@code Resource} type.
 * @param <D> the destination {@code Resource} type.
 */
public interface Transfer<S extends Resource, D extends Resource> {
  
}
